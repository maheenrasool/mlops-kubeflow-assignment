name: mlops-mlflow-pipeline

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      data_path: {type: str, default: "data/raw_data.csv"}
    command: >
      python src/run_pipeline.py --data_path {data_path}